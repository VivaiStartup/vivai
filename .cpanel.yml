---
deployment:
  tasks:
    - export FRONT=/home/vivaiit/public_html/
    - export APIAPP=/home/vivaiit/project/live-api/

    - /bin/mkdir -p $FRONT
    - /bin/mkdir -p $APIAPP

    - /bin/cp -R vivai/dist/. $FRONT
    - /bin/cp -R vivai-api/. $APIAPP